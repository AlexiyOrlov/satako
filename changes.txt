Neoforge: added block rendering method;

fixed scroll area's title;

fixed configuration returning integer instead of float from "getFloat"
fixed scroll area's scrollbar's horizontal position;

fixed item deletion;